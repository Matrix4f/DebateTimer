{
  "name": "debate-timer",
  "version": "1.0.0",
  "description": "A digital timer for competitive debate",
  "main": "index.js",
  "dependencies": {
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1",
    "popper": "^1.0.1"
  },
  "devDependencies": {
	"electron": "^3.0.0",
    "electron-packager": "^12.2.0"
  },
  "scripts": {
    "start": "electron src\index.js",
    "buildDev": "electron-packager . DebateTimer --overwrite --asar=true --icon=icon.ico --platform=win32 --arch=ia32 --prune=true --out=release-builds --version-string.CompanyName=SohamGovande --version-string.FileDescription=ddd --version-string.ProductName=\"Debate Timer\"",
    "build": "electron-packager . DebateTimer --overwrite --platform=win32 --ignore=node_modules/electron* --app-version=1.0.0"
  },
  "author": "Soham Govande",
  "license": "ISC"
}
